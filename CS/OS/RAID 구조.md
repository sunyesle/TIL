# RAID 구조
**RAID**(Redundant Array of Independent Disks)는 여러 개의 디스크를 묶어 신뢰성과 성능을 향상시키는 기술이다.

## 중복성을 통한 신뢰성 향상
### 미러링(Mirroring)
**미러링**은 같은 데이터를 여러 디스크에 동시에 저장하는 방식이다.
디스크 하나가 고장 나도 다른 디스크에 데이터가 존재하기 때문에 데이터를 보존할 수 있다.

### 패리티(Parity)
**패리티**는 XOR(배타적 논리합) 연산을 이용해 여러 데이터의 패리티 정보를 계산하여 별도로 저장하는 방식이다.
디스크 하나가 고장 나도 다른 디스크의 데이터와 패리티 정보를 이용해 손실된 데이터를 복구할 수 있다.

패리티 정보는 XOR의 다음 성질을 이용한다.
```txt
A XOR A = 0 // 자기 자신과 XOR하면 0
A XOR 0 = A // 0과 XOR하면 그대로
```

데이터 A, B, C를 저장한다면 패리티 정보 P는 다음과 같이 계산한다.
```txt
P = A XOR B XOR C
```

여러 데이터 중 하나가 손실되었을 때, 나머지 데이터와 패리티 정보를 XOR 하면 손실된 데이터를 계산할 수 있다.
예를 들어 B가 손실되었다면, A, C, P를 이용해 B를 복구할 수 있다.
```txt
A XOR C XOR P
= A XOR C XOR (A XOR B XOR C)
= (A XOR A) XOR B XOR (C XOR C)
= 0 XOR B XOR 0
= B
```

### 비교
|  | 미러링 | 패리티 |
|---|---|---|
| **방식** | 데이터 1:1 완전 복제 | XOR 연산을 통한 복구 값 저장 |
| **공간 효율성** | 낮음(50%) | 높음(디스크가 늘어날수록 효율 증가) |
| **쓰기 성능** |복제만 하면 되므로 비교적 높음 | 패리티 연산으로 인해 상대적으로 낮음 |

## 병렬 처리를 통한 성능 향상

### 스트라이핑(Striping)
**스트라이핑**은 하나의 데이터를 여러 디스크에 나누어 저장하여, 여러 디스크가 데이터를 병렬로 처리할 수 있도록 하는 방식이다.
데이터를 나누어 저장하는 단위에 따라 비트 레벨, 바이트 레벨, 블록 레벨 스트라이핑으로 구분할 수 있다.

## RAID 레벨


| | 특징 | 신뢰성 | 성능 | 디스크 효율성 |
|---|---|---|---|---|
| **RAID 0** | 스트라이핑만 사용(중복성 없음) | 최저 | 최고 | 최고(100%)
| **RAID 1** | 미러링(전체 복사본 저장) | 최고 | 읽기 높음 / 쓰기 보통 | 최저(50%) |
| **RAID 5** | 분산된 패리티를 이용한 스트라이핑 | 높음(단일 디스크 고장 허용) | 읽기 높음 / 쓰기 보통 | 높음(67~94%, n-1/n) |
| **RAID 6** | 이중 분산 패리티를 이용한 스트라이핑 | 매우 높음(2개의 디스크 고장 허용) | 읽기 보통 / 쓰기 느림 | 보통(50~88%, n-2/n) |
| **RAID 10** | 스트라이핑과 미러링의 결합(RAID 1+0) | 매우 높음 | 최고 | 최저(50%) |

## RAID 0+1과 1+0의 차이점
<img width="320" height="320" alt="다운로드 (1)" src="https://github.com/user-attachments/assets/6782a8ae-c25d-4359-94ca-c6dffacc5349" />
<img width="320" height="320" alt="다운로드 (2)" src="https://github.com/user-attachments/assets/f5158d37-73e8-4576-90f1-f4690481ee16" />

<br>
<br>

**RAID 01**은 스트라이핑(0)된 그룹을 미러링(1)한다.<br>
스트라이프 세트에서 디스크 하나에 장애가 발생하면 해당 세트 전체가 손실된 것으로 간주된다. 각 미러 세트에서 동시에 디스크 하나씩 장애가 발생하는 경우 두 스트라이프 세트가 모두 손상되어 데이터를 정상적으로 복구할 수 없다.

**RAID 10**은 미러링(1)된 그룹을 스트라이핑(0)한다.<br>
서로 다른 미러 그룹에서 각각 하나씩 디스크에 장애가 발생하더라도 각 미러 그룹에 데이터 복사본이 남아 있어 전체 데이터를 유지할 수 있다.

안정성이 높은 RAID 10이 RAID 01보다 더 많이 사용된다.

<img width="551" height="625" alt="10_12_RAID_01" src="https://github.com/user-attachments/assets/eb542394-2285-40a7-8a09-fa4e371f602b" />

---
**Reference**
- https://tech.gluesys.com/blog/2020/07/22/storage_6_intro.html
- https://soft.plusblog.co.kr/125
- https://yonghwankim-dev.tistory.com/494
- https://wikidocs.net/312494

# k6를 활용한 성능 테스트 및 모니터링 환경 구축
k6는 Grafana Labs에서 만든 오픈소스 성능 테스트 도구로, JavaScript 기반으로 테스트 시나리오를 작성하여 여러 사용자의 동시 요청을 시뮬레이션 할 수 있다.

## k6 설치
다음 명령어를 통해 설치할 수 있다.

**Windows**
```bash
winget install k6 --source winget
```
**MacOS**
```bash
brew install k6
```

## 모니터링 인프라 구축 (Grafana & Prometheus)
k6 성능 테스트를 OpenTelemetry와 통합한다. 테스트 메트릭은 OpenTelemetry 수집기(OTEL Collector)로 전송되고, 수집기는 이를 Prometheus에 전달하여 저장한다. 이렇게 저장된 메트릭은 Grafana 대시보드를 사용하여 시각화할 수 있다.

> docker-compose.yml
```yml
networks:
  k6:

services:
  prometheus:
    image: prom/prometheus:v3.11.0@sha256:dda13e28bf95a5e5ca5b8ed56852006094c1c8e8871d9c9dbeed30aa6e55271f
    command:
      - --web.enable-remote-write-receiver
      - --enable-feature=native-histograms
      - --config.file=/etc/prometheus/prometheus.yml
    networks:
      - k6
    ports:
      - "9090:9090"

  alloy:
    image: grafana/alloy:v1.15.0@sha256:579582bce40049f2e27bd1e445000689c907a41f0495cb0d0c7f65609316ba7c
    networks:
      - k6
    volumes:
      - ./docker/grafana/alloy:/etc/alloy
    environment:
      REMOTE_WRITE_HOST: prometheus:9090
    depends_on:
      - prometheus
    command:
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy/data
      - --server.http.listen-addr=0.0.0.0:12345
      - --stability.level=experimental # Enable all functionality
    ports:
      - "12345:12345" # Alloy UI
      - "4317:4317" # gRPC receiver
      - "4318:4318" # HTTP receiver

  grafana:
    image: grafana/grafana:12.4.2@sha256:83749231c3835e390a3144e5e940203e42b9589761f20ef3169c716e734ad505
    networks:
      - k6
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_BASIC_ENABLED=false
    volumes:
      - ./docker/grafana:/etc/grafana/provisioning/
```

**k6 대시보드 접속**: http://localhost:3000/d/demo-uid/k6-opentelemetry-prometheus

## Spring Boot 메트릭 전송 설정
> build.gradle
```gradle
dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'org.springframework.boot:spring-boot-starter-opentelemetry'
	testImplementation 'org.springframework.boot:spring-boot-starter-actuator-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-opentelemetry-test'
}
```

> application.yml
```yml
management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics"
  otlp:
    metrics:
      export:
        url: http://localhost:4318/v1/metrics
```

## k6 스크립트 작성
테스트 대상이 될 간단한 API이다.
```java
@RestController
public class PingController {

    @GetMapping("/api/ping")
    public String ping() {
        return "ok";
    }
}
```

위 API를 테스트하는 k6 스크립트이다. 가상 유저 5명이 10초 동안 0.5초 간격으로 API를 호출하게 된다.
```js
import http from 'k6/http';
import { sleep } from 'k6';

export const options = {
    vus: 5, // 가상 유저 수
    duration: '10s', // 테스트 시간
};

// 가상 유저가 반복해서 실행할 함수
export default function () {
    http.get('http://localhost:8080/api/ping'); // API 호출
    sleep(0.5); // 0.5초 대기
}
```

## 테스트 실행
```bash
K6_OTEL_GRPC_EXPORTER_ENDPOINT=localhost:4317 \
K6_OTEL_GRPC_EXPORTER_INSECURE=true \
K6_OTEL_METRIC_PREFIX=k6_ \
k6 run --tag testid=0 -o experimental-opentelemetry script.js
```

## 실행 결과 확인
성능 테스트를 실행하면 터미널에 아래와 같이 결과가 출력되며, 메트릭이 Grafana로 전송된다.
```bash
         /\      Grafana   /‾‾/                                            
    /\  /  \     |\  __   /  /                                             
   /  \/    \    | |/ /  /   ‾‾\                                           
  /          \   |   (  |  (‾)  |                                          
 / __________ \  |_|\_\  \_____/ 

WARN[0000] OpenTelemetry output has been graduated as a stable output.You can now use just "opentelemetry" instead of "experimental-opentelemetry". The experimental version will be removed in future versions. 

     execution: local
        script: script.js
        output: opentelemetry (grpc (insecure), localhost:4317)

     scenarios: (100.00%) 1 scenario, 5 max VUs, 40s max duration (incl. graceful stop):
              * default: 5 looping VUs for 10s (gracefulStop: 30s)



  █ TOTAL RESULTS 

    HTTP
    http_req_duration..............: avg=3.53ms   min=514.1µs  med=2ms      max=26ms     p(90)=10.77ms  p(95)=12.51ms 
      { expected_response:true }...: avg=3.53ms   min=514.1µs  med=2ms      max=26ms     p(90)=10.77ms  p(95)=12.51ms 
    http_req_failed................: 0.00%  0 out of 100
    http_reqs......................: 100    9.856961/s

    EXECUTION
    iteration_duration.............: avg=505.81ms min=501.02ms med=503.14ms max=529.32ms p(90)=513.22ms p(95)=528.74ms
    iterations.....................: 100    9.856961/s
    vus............................: 5      min=5        max=5
    vus_max........................: 5      min=5        max=5

    NETWORK
    data_received..................: 12 kB  1.1 kB/s
    data_sent......................: 7.8 kB 769 B/s




running (10.1s), 0/5 VUs, 100 complete and 0 interrupted iterations
default ✓ [======================================] 5 VUs  10s
```

| Name | 설명 |
|---|---|
| `http_req_duration` | 요청 시작부터 응답을 완료할 때까지 걸린 총 시간 |
| `http_req_failed` | 실패한 요청의 비율 |
| `http_reqs` | k6가 생성한 총 HTTP 요청 수 |
| `iteration_duration` | 시나리오 함수 한 번을 완전히 실행하는 데 걸린 시간 |
| `iterations` | 가상 유저가 시나리오 함수를 실행한 총 횟수 |
| `vus` | 현재 활성화되어 실시간으로 실행 중인 가상 유저 수 |
| `vus_max` | 테스트 기간 동안 할당된 최대 가상 유저 수 |
| `data_received` | k6가 서버로부터 수신한 총 데이터 양 |
| `data_sent` | k6가 서버로 전송한 총 데이터 양 |

<br>

**대시보드 화면**

<img width="1508" height="918" alt="grafana" src="https://github.com/user-attachments/assets/61f26c73-eab5-4a3c-be8b-1b997d0a93ef" />


---
**Reference**
- https://github.com/grafana/k6/blob/master/examples/docker-compose/opentelemetry/docker-compose.yml
